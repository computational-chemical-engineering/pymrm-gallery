# Runtime for gallery pages and CI.
pymrm>=2.3
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
PyYAML>=6.0
sympy>=1.13          # J4.8 proves ASM1 continuity symbolically, not at a sample point

# Notebook execution (CI only)
nbformat>=5.10
nbclient>=0.10
ipykernel>=6.29
