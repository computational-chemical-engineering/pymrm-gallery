# Decisions owed by the maintainer that are not tied to one catalogue case.
#
# Per-case blockers live in queue_cases/<ID>.yaml and reach the decisions
# dashboard from there. These are repo-wide: they do not belong to any one case,
# so nothing would carry them forward otherwise, and they were previously being
# restated by hand in each session's prompt.
#
# Rendered at the top of docs/dashboards/needs-input.html. Delete an entry once
# it is settled, and record what was decided in docs/handoff.md.
#
# Fields: id, title, question, detail, blocking (true if work is stalled on it),
#         raised (date).

# ALL THREE STANDING DECISIONS WERE ANSWERED ON 2026-08-02 and are recorded in
# docs/handoff.md. Nothing repo-wide is outstanding.
#
#   textbook-canonical-sources -> YES. The ~20 P1/T0 cases whose result is a
#     closed form nobody disagrees about may be built without the original
#     paper. See AGENTS.md, "Published work only", for the standard that still
#     applies: a page written from memory is still the forbidden thing, so name
#     what the model was read from even when it is a monograph.
#
#   history-purge -> YES, done. git-filter-repo removed the three overlay PNGs
#     and stripped every base64 image payload from five commits of
#     docs/dashboards/needs-input.html; force-pushed 2026-08-02. All 66 commits
#     preserved, the tip byte-identical (591 files, same blob hashes), .git
#     27 MB -> 14 MB. Backup bundle at ~/pymrm-gallery-backup-20260802-1803.
#     ANY OTHER CLONE MUST `git fetch && git reset --hard origin/main` - a plain
#     pull would merge the old history back and reintroduce the images.
#
#   elsevier-key-permission -> ALLOWED. The key at ~/.config/elsevier/apikey
#     reads and the PII endpoint answers (verified end to end on
#     0009250970850734, 47 kB). A durable permission rule in
#     ~/.claude/settings.json is still worth adding - the agent is blocked from
#     editing its own permissions, by design - but nothing is stalled without
#     it. Standing caveat from docs/handoff.md is unchanged: the API text is
#     good for prose and unusable for numbers on pre-1980 scans.

- id: operator-free-audit-pages
  title: "Are operator-free audit pages in scope? The structures-empty precedent"
  raised: '2026-08-09'
  blocking: false
  question: >
    H1.8 (Robeson upper bound) is the first published page with an empty
    `structures` list AND an empty `pymrm_api`. It is a correlation audit:
    there is no PDE, no grid and no pymrm operator anywhere in it, and its
    "PyMRM implementation" section says exactly that rather than manufacturing
    a model to justify its presence. Is that in scope for the gallery, and if
    so should such pages be marked as a distinct kind?
  detail: >
    Both of H1.8's verifiers judged the empty lists HONEST rather than evasive -
    the alternative was to invent a discretised model the source does not
    motivate, which is the worse failure. It is also safe mechanically:
    scripts/check_metadata.py does not constrain the contents of `structures`,
    nothing in the site build reads it, and the page's front matter carries no
    `struct:` tag.
    So nothing is stalled - this is a question about what the gallery is FOR,
    not about H1.8's correctness. It is raised now because it will recur: the
    remaining empirical-correlation cases in the queue land in the same shape,
    and it is better decided deliberately once than by default several times.
    Three ways to go: (a) in scope, leave as is; (b) in scope but tag them, so
    a reader can tell an audit page from a model page at a glance and the
    catalogue can count them separately; (c) out of scope, in which case H1.8
    should carry a note saying why it is an exception.
    The page: pages/H1.8-robeson-upper-bound/ (live on the site under section H).

- id: j2.3-nyvlt-scope
  title: "J2.3 (Nyvlt 1968) is figure-only — which route, or defer?"
  raised: '2026-08-09'
  blocking: false
  question: >
    A pre-dispatch render on 2026-08-09 settled J2.3's table-vs-figure question
    the wrong way: the paper prints NO table. Its catalogued route - extract the
    nucleation order m and constant k from measured metastable-zone widths -
    therefore needs load-bearing digitisation of the authors' own plots. Build
    the structure-only version, digitise and send overlays for review, or defer?
  detail: >
    What is actually printed: the primary data are plotted points on the log-log
    Figs. 2-5 (four salts), and the fitted k and m per substance appear only as
    scatter in Figs. 6-10. The paper states its own dataset "of more than 25
    substances were treated by the method of least squares" - results shown,
    numbers never printed.
    Digitising them would be load-bearing, so the page would stall at the review
    gate (the A3.14 precedent), and A3.12's owed figure review is already open on
    this dashboard - which is the reason to ask rather than just build it.
    The honest alternative needs no digitisation and no maintainer: prove the
    printed eq. (12)-(16) chain, which closes to
    log b = (m-1)log(...) - log a + log k + m log(dt_max) so that the SLOPE IS m,
    then measure what the paper's own three-cooling-rate design can and cannot
    resolve about m. That is a J1.4-shaped page and is recorded in
    queue_cases/J2.3.yaml.
    Either way it is no longer the "small S1" the 2026-08-08 model-split note
    assumed, so it should not be picked up on that basis. Nothing is stalled:
    the case is parked `unclaimed` with both routes written down.
    The paper: ~/papers/pymrm-gallery/Nyvlt1968-nucleation-kinetics-solutions-JCrystGrowth3-377.pdf

- id: d1x-fixed-bed-scope
  title: "D1.1-D1.5: five buildable cases held behind one open scope question"
  raised: '2026-08-15'
  blocking: true
  question: >
    The D1.x ladder (one-dimensional fixed-bed models: basic, with axial mixing,
    with interfacial gradients, with intraparticle gradients, and the
    two-dimensional pseudohomogeneous model) has been held since 2026-08-07
    pending your scope decision. Every other T0/P1 case with a source on disk is
    now published, so this is the single highest-leverage answer available:
    saying yes unblocks FIVE cases at once. Are they in scope, and if so should
    they be five pages or one page with five configurations?
  detail: >
    Nothing is missing technically. All five sources are on disk and their E1.1
    verdicts are already recorded in docs/papers-inventory.yaml against Froment,
    De Wilde & Bischoff sections 11.5 (basic 1-D, with a worked pressure-drop
    example and the runaway criteria that the published D2.2 page reproduces),
    11.6 (axial mixing - the S4 structure exactly, with Danckwerts conditions),
    11.7 (two-dimensional pseudohomogeneous, which papers-requested.yaml called
    the genuine gap in the gallery), 11.8 (interfacial gradients, with a worked
    gas-solid example) and 11.9 (interfacial AND intraparticle, with three
    worked industrial examples - a steam reformer, a butene dehydrogenation
    reactor, and catalytic reforming).
    Two things make the shape worth deciding deliberately rather than by
    default. First, the five sections are a deliberate LADDER in the book: each
    adds one transport mechanism to the previous, so five separate pages would
    repeat a great deal of assembly, while one page could show what each rung
    actually buys - which is closer to what the ladder is for. Second, the
    2026-08-13/15 batch established that a page comparing models is far more
    valuable than a page restating one (J4.1 found Contois and Monod are the
    same model on constant-yield runs; F3.2 found an approximation's stated
    assumption does not predict where it fails), and the D1.x ladder is the
    largest such comparison available in the queue.
    THE COST OF NOT ANSWERING: these five plus J2.3 are the entire unblocked
    remainder at T0/P1. Without a decision the next session has no case it can
    build without either drawing on the Fable reserve or dropping to T1.
    The book: ~/papers/pymrm-gallery/BOOK-FromentDeWildeBischoff-2011-chemical-reactor-analysis-and-design-3ed.pdf
    The cases: queue_cases/D1.1.yaml through queue_cases/D1.5.yaml
